import { Fragment } from "react";
import PaystackCallback from "@components/pages/callbacks/paystack-callback";

const Page = () => {
    return (
        <Fragment>
            <PaystackCallback />
        </Fragment>
    );
};

export default Page;
