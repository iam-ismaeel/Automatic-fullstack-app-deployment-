import { Fragment } from "react";
import AuthorizeCallback from "@/components/pages/callbacks/authorize-callback";

const Page = () => {
  return (
    <Fragment>
      <AuthorizeCallback />
    </Fragment>
  );
};

export default Page;
