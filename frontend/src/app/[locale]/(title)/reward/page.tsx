import React, { Fragment } from "react";
import Reward from "@/components/pages/reward/page";

const page = () => {
  return (
    <Fragment>
      <Reward/>
    </Fragment>
  );
};

export default page;
